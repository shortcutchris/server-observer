# Server Observer 0.3.1

Das Projekt-Frontend ist jetzt nur noch einen Klick entfernt:

- neuer „Frontend öffnen“-Button in Projektkarten und Detailansicht
- Auswahlmenü bei mehreren Weboberflächen
- gestoppte Projekte mit „Starten & öffnen“ starten
- Browser öffnet erst, wenn das Frontend per HTTP erreichbar ist
- bevorzugtes Ziel über `frontend: true` in `.server-observer.yml`
- zusätzlicher Frontend-Schnellzugriff in der Menüleiste
